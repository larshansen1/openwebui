# PostgreSQL Password
# SECURITY: Generate strong password (min 32 characters)
# Command: openssl rand -base64 32
POSTGRES_PASSWORD=

# OpenWebUI Secret Key
# CRITICAL: Used for JWT signing and session management
# SECURITY: Generate strong key (min 64 characters)
# Command: openssl rand -hex 32
# WARNING: Changing this invalidates all existing user sessions
WEBUI_SECRET_KEY=

# Optional: OpenAI API Configuration
OPENAI_API_KEY=
OPENAI_API_BASE_URL=https://openrouter.ai/api/v1

# Qdrant Vector Database Authentication
# SECURITY: Generate strong API key (min 32 characters)
# Command: openssl rand -base64 32
# Leave empty to disable authentication (NOT recommended for production)
QDRANT_API_KEY=

# Ollama Configuration
# Note: Changing the embedding model requires recreating Qdrant collections
# as different models produce different dimensional vectors
# nomic-embed-text: 768 dims, 2048 context, slower but better quality
# all-minilm: 384 dims, 512 context, much faster but lower quality
# mxbai-embed-large: 1024 dims, 512 context, best quality but slowest
RAG_EMBEDDING_MODEL=all-minilm

# MCP Server Configuration
# SECURITY: Generate strong API key (min 32 characters)
# Command: openssl rand -base64 32
# This key protects access to all MCP server endpoints
MCP_API_KEY=

# Brave Search API Key
# Get from: https://brave.com/search/api/
# Leave empty if not using brave-search MCP server
BRAVE_API_KEY=

# Obsidian Vault Configuration
# Path to Obsidian vault
# Absolute path on host filesystem (e.g., /Users/username/Documents/Obsidian/Vault)
# Prod: Path within syncthing-data volume (e.g., /vault)
# This path will be mounted to the mcp-server container for obsidian access
# The vault must exist before starting the service
OBSIDIAN_VAULT_PATH=

# ============================================================================
# Monitoring Configuration (Optional but Recommended for Production)
# ============================================================================

# Telegram Bot for Alerts
# Reuse your existing infrastructure monitoring bot
# Get bot token from @BotFather on Telegram
# Get chat ID by sending /start to @userinfobot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Grafana Admin Password
# SECURITY: Set a strong password for Grafana web UI access
# Command: openssl rand -base64 24
# Access Grafana via SSH tunnel: ssh -L 3001:localhost:3001 user@server
GRAFANA_ADMIN_PASSWORD=

# Backup Encryption Key (Optional)
# SECURITY: Passphrase for encrypting .env and database backups
# Default: "openwebui-backup" (if not set)
# Production: Set a strong custom passphrase
# Command: openssl rand -base64 32
# Usage: BACKUP_ENCRYPTION_KEY="your-secret" ./scripts/backup.sh
# Note: Store this passphrase securely - needed to restore encrypted backups
BACKUP_ENCRYPTION_KEY=
